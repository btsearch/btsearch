export const TERRAIN_PROFILE_MAX_DISTANCE_M = Math.max(1_000, Number(process.env.TERRAIN_PROFILE_MAX_DISTANCE_M) || 30_000);
export const TERRAIN_UPSTREAM_TIMEOUT_MS = Math.max(1_000, Number(process.env.TERRAIN_UPSTREAM_TIMEOUT_MS) || 12_000);
